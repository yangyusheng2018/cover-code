## 1. 规格梳理

- [x] 1.1 盘点 monorepo 目录与根脚本，写入 `monorepo-workspace` spec
- [x] 1.2 盘点 sdk 四包 API 与插件行为，写入 `istanbul-live-sdk` spec
- [x] 1.3 盘点 examples 工程，写入 `coverage-demo-examples` spec

## 2. 管理端后端规格

- [x] 2.1 对照 `docs/API.md` 认证与用户模块，写入 `cover-admin-auth` spec
- [x] 2.2 对照覆盖率上报与 branch-coverages 章节，写入 `cover-admin-coverage` spec

## 3. 管理端前端规格

- [x] 3.1 对照 router 与权限门禁，写入 `cover-admin-dashboard` spec

## 4. 归档与校验

- [x] 4.1 同步 delta spec 至 `openspec/specs/`
- [x] 4.2 撰写 verify.md 与 retrospective.md
- [x] 4.3 移入 `openspec/changes/archive/2026-06-11-cover-platform-baseline/`
